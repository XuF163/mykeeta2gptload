# Copy this to `.env` (do NOT commit `.env`) and fill in the required values.
#
# Docker Compose automatically loads `.env` for variable substitution.

# Required: GPTMail API key for receiving OTP emails
GPTMAIL_API_KEY=

# Optional but recommended: protects gpt-load UI / management API.
# If you want mykeeta -> gpt-load auto-import, this must be set (both sides share it).
GPT_LOAD_AUTH_KEY=

# Optional: gpt-load group name to import into
GPT_LOAD_GROUP_NAME=#pinhaofan

# Optional: gpt-load DB password (Postgres). Defaults to 123456 if empty.
POSTGRES_PASSWORD=123456

# Optional: encrypt API keys at rest in gpt-load DB (set a long random string)
GPT_LOAD_ENCRYPTION_KEY=

# Optional: timezone used by gpt-load logs
TZ=Asia/Shanghai

